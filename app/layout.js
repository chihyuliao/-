import "./globals.css";

export const metadata = {
  title: "我讀字升級",
 
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}
    
    </body>
    </html>
  )
}
